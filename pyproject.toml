[project]
name = "myst-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Martín René", email = "martinvilu@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
myst-tools = "myst_tools:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
